{
  "name": "sacoparents-app",
  "version": "1.0.0",
  "private": true,
  "description": "SA Coparents — Relational Mediation Prep. Monorepo manifest. The actual React + Capacitor app lives in /frontend, the FastAPI backend in /backend. This root manifest exists so CI tools (Codemagic, GitHub Actions) can detect the project as a Node workspace.",
  "scripts": {
    "build": "cd frontend && yarn build",
    "install:frontend": "cd frontend && yarn install --frozen-lockfile",
    "cap:sync": "cd frontend && npx cap sync android",
    "android:bundle": "cd frontend/android && ./gradlew bundleRelease"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mattsossi-cmyk/sacoparents-app"
  },
  "license": "UNLICENSED"
}
